# ================================================
# SECURITY ENVIRONMENT VARIABLES
# ================================================
# Add these to your existing .env file
# ================================================

# -----------------
# Token Security
# -----------------
# Secret for signing unsubscribe tokens and other security tokens
# REQUIRED: Generate a strong random secret
# Command: openssl rand -base64 32
# Or use your existing BETTER_AUTH_SECRET (it will fall back to this)
UNSUBSCRIBE_TOKEN_SECRET=REPLACE_WITH_32_CHAR_RANDOM_STRING

# -----------------
# Environment
# -----------------
# Ensure this is set correctly for SSL and logging behavior
NODE_ENV=production  # Use 'development' for local testing

# -----------------
# Database (Neon)
# -----------------
# Your existing Neon connection string
# SSL will be enforced in production automatically
NEON_DATABASE=postgresql://...

# -----------------
# Authentication
# -----------------
# Your existing Better Auth secret
# Will be used as fallback for token signing if UNSUBSCRIBE_TOKEN_SECRET not set
BETTER_AUTH_SECRET=your-existing-secret
BETTER_AUTH_URL=https://your-domain.com

# -----------------
# PayPal
# -----------------
# Your existing PayPal credentials
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-client-secret
PAYPAL_MODE=sandbox  # or 'live' for production

# -----------------
# Email (Resend)
# -----------------
# Your existing Resend API key
RESEND_API_KEY=your-resend-api-key

# -----------------
# Redis (Upstash)
# -----------------
# Your existing Redis credentials
UPSTASH_REDIS_REST_URL=your-redis-url
UPSTASH_REDIS_REST_TOKEN=your-redis-token

# ================================================
# SECURITY NOTES
# ================================================
# 1. Never commit .env files to version control
# 2. Use different secrets for development and production
# 3. Rotate secrets regularly (every 90 days recommended)
# 4. Keep secrets at least 32 characters long
# 5. Use a password manager to store production secrets
# ================================================
