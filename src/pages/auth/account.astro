---
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/Navbar.astro';
import { pool } from '../../lib/db';

const user = Astro.locals.user;

// If not logged in, redirect to login page
if (!user) {
  return Astro.redirect('/auth/login');
}

// Fetch user addresses
let userAddresses: any[] = [];
const client = await pool.connect();
try {
  const addressResult = await client.query(
    'SELECT * FROM "userAddresses" WHERE "userId" = $1 ORDER BY "isDefault" DESC, "createdAt" DESC',
    [user.id]
  );
  userAddresses = addressResult.rows;
} catch (error) {
  console.error('Error fetching addresses:', error);
} finally {
  client.release();
}

// Fetch active shipping profiles and user's selection
let shippingProfiles: any[] = [];
let selectedShippingId: string | null = null;
const shippingClient = await pool.connect();
try {
  // Get all active shipping profiles
  const profilesResult = await shippingClient.query(
    'SELECT * FROM "shippingProfiles" WHERE "isActive" = true ORDER BY "displayOrder", "createdAt"'
  );
  shippingProfiles = profilesResult.rows.map(row => ({
    ...row,
    basePrice: parseFloat(row.basePrice),
    freeShippingThreshold: row.freeShippingThreshold ? parseFloat(row.freeShippingThreshold) : null,
  }));

  // Get user's selected shipping profile
  const selectionResult = await shippingClient.query(
    'SELECT "shippingProfileId" FROM "userCartShipping" WHERE "userId" = $1',
    [user.id]
  );

  if (selectionResult.rows.length > 0) {
    selectedShippingId = selectionResult.rows[0].shippingProfileId;
  } else {
    // Use default if no selection exists
    const defaultProfile = shippingProfiles.find((p) => p.isDefault);
    selectedShippingId = defaultProfile?.id || shippingProfiles[0]?.id;
  }
} catch (error) {
  console.error('Error fetching shipping profiles:', error);
} finally {
  shippingClient.release();
}

---

<Layout title="Mein Konto - DTF Transfer Print">
  <div class="min-h-screen">
    <Navbar />
    <div class="py-4 px-4 sm:px-6 lg:px-8">
    <div class="max-w-7xl lg:max-w-7xl-extended mx-auto">
      <!-- Header -->
      <div class="mb-4">
        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
          <div>
            <h1 class="text-2xl sm:text-3xl font-bold text-base-content bg-clip-text">
              Mein Konto
            </h1>
            <p class="text-sm text-base-content/60 mt-1">Willkommen zurück, {user.name}</p>
          </div>
          <button id="logout-button" class="btn btn-outline btn-error btn-sm gap-2">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
              <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 9V5.25A2.25 2.25 0 0 0 13.5 3h-6a2.25 2.25 0 0 0-2.25 2.25v13.5A2.25 2.25 0 0 0 7.5 21h6a2.25 2.25 0 0 0 2.25-2.25V15m3 0 3-3m0 0-3-3m3 3H9" />
            </svg>
            Abmelden
          </button>
        </div>
      </div>

      <!-- Profile content -->
      <div class="bg-base-100/80 backdrop-blur-xl shadow-2xl rounded-2xl overflow-hidden border border-base-300/20">
        <div class="p-4 sm:p-5">
          <!-- Profile -->
          <div class="space-y-4">
            <div class="space-y-4">
              <!-- Profile Information -->
              <div class="card bg-base-200/50 shadow-md border border-base-300/30">
                <div class="card-body p-4">
                  <h2 class="card-title text-lg mb-3 flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-primary">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 6a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0ZM4.501 20.118a7.5 7.5 0 0 1 14.998 0A17.933 17.933 0 0 1 12 21.75c-2.676 0-5.216-.584-7.499-1.632Z" />
                    </svg>
                    Profil-Informationen
                  </h2>
                  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div class="form-control">
                      <label class="label py-1">
                        <span class="label-text text-sm font-medium">Name</span>
                      </label>
                      <input
                        type="text"
                        value={user.name}
                        class="input input-bordered input-sm"
                        readonly
                      />
                    </div>
                    <div class="form-control">
                      <label class="label py-1">
                        <span class="label-text text-sm font-medium">E-Mail</span>
                      </label>
                      <input
                        type="email"
                        value={user.email}
                        class="input input-bordered input-sm"
                        readonly
                      />
                    </div>
                    <div class="form-control">
                      <label class="label py-1">
                        <span class="label-text text-sm font-medium">E-Mail Status</span>
                      </label>
                      <div class="flex items-center gap-2 pt-2">
                        {user.emailVerified ? (
                          <span class="badge badge-success badge-sm gap-1.5">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-3.5 h-3.5">
                              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
                            </svg>
                            Verifiziert
                          </span>
                        ) : (
                          <span class="badge badge-warning badge-sm gap-1.5">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-3.5 h-3.5">
                              <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m9-.75a9 9 0 1 1-18 0 9 9 0 0 1 18 0Zm-9 3.75h.008v.008H12v-.008Z" />
                            </svg>
                            Nicht verifiziert
                          </span>
                        )}
                      </div>
                    </div>
                    <div class="form-control">
                      <label class="label py-1">
                        <span class="label-text text-sm font-medium">Konto Status</span>
                      </label>
                      <div class="flex items-center gap-2 pt-2">
                        {user.accountLocked ? (
                          <span class="badge badge-error badge-sm gap-1.5">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-3.5 h-3.5">
                              <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 10.5V6.75a4.5 4.5 0 1 0-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 0 0 2.25-2.25v-6.75a2.25 2.25 0 0 0-2.25-2.25H6.75a2.25 2.25 0 0 0-2.25 2.25v6.75a2.25 2.25 0 0 0 2.25 2.25Z" />
                            </svg>
                            Gesperrt
                          </span>
                        ) : (
                          <span class="badge badge-success badge-sm gap-1.5">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-3.5 h-3.5">
                              <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 10.5V6.75a4.5 4.5 0 1 1 9 0v3.75M3.75 21.75h10.5a2.25 2.25 0 0 0 2.25-2.25v-6.75a2.25 2.25 0 0 0-2.25-2.25H3.75a2.25 2.25 0 0 0-2.25 2.25v6.75a2.25 2.25 0 0 0 2.25 2.25Z" />
                            </svg>
                            Aktiv
                          </span>
                        )}
                      </div>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Account Details -->
              <div class="card bg-base-200/50 shadow-md border border-base-300/30">
                <div class="card-body p-4">
                  <h2 class="card-title text-lg mb-3 flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-primary">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
                    </svg>
                    Konto-Details
                  </h2>
                  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                      <label class="label py-1">
                        <span class="label-text text-sm font-medium text-base-content/70">Konto erstellt</span>
                      </label>
                      <p class="text-sm text-base-content">
                        {new Date(user.createdAt).toLocaleDateString('de-DE', {
                          year: 'numeric',
                          month: 'long',
                          day: 'numeric',
                        })}
                      </p>
                    </div>
                    {user.lastLoginAt && (
                      <div>
                        <label class="label py-1">
                          <span class="label-text text-sm font-medium text-base-content/70">Letzter Login</span>
                        </label>
                        <p class="text-sm text-base-content">
                          {new Date(user.lastLoginAt).toLocaleDateString('de-DE', {
                            year: 'numeric',
                            month: 'long',
                            day: 'numeric',
                            hour: '2-digit',
                            minute: '2-digit',
                          })}
                        </p>
                      </div>
                    )}
                  </div>
                </div>
              </div>

              <!-- Email Preferences -->
              <div class="card bg-base-200/50 shadow-md border border-base-300/30">
                <div class="card-body p-4">
                  <h2 class="card-title text-lg mb-3 flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-primary">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 0 1-2.25 2.25h-15a2.25 2.25 0 0 1-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0 0 19.5 4.5h-15a2.25 2.25 0 0 0-2.25 2.25m19.5 0v.243a2.25 2.25 0 0 1-1.07 1.916l-7.5 4.615a2.25 2.25 0 0 1-2.36 0L3.32 8.91a2.25 2.25 0 0 1-1.07-1.916V6.75" />
                    </svg>
                    E-Mail Einstellungen
                  </h2>
                  <div class="form-control">
                    <label class="label cursor-pointer justify-start gap-3 py-1">
                      <input
                        type="checkbox"
                        checked={user.emailNotifications}
                        class="checkbox checkbox-primary checkbox-sm"
                      />
                      <div>
                        <span class="label-text text-sm font-medium">E-Mail-Benachrichtigungen empfangen</span>
                        <p class="text-xs text-base-content/60 mt-0.5">Erhalten Sie Updates zu Ihren Bestellungen und Angeboten</p>
                      </div>
                    </label>
                  </div>
                </div>
              </div>

              <!-- Business Information -->
              <div class="card bg-base-200/50 shadow-md border border-base-300/30">
                <div class="card-body p-4">
                  <h2 class="card-title text-lg mb-3 flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-primary">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 21h16.5M4.5 3h15M5.25 3v18m13.5-18v18M9 6.75h1.5m-1.5 3h1.5m-1.5 3h1.5m3-6H15m-1.5 3H15m-1.5 3H15M9 21v-3.375c0-.621.504-1.125 1.125-1.125h3.75c.621 0 1.125.504 1.125 1.125V21" />
                    </svg>
                    Geschäftsinformationen
                  </h2>
                  <form id="business-info-form" class="space-y-4">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                      <div class="form-control">
                        <label class="label py-1">
                          <span class="label-text text-sm font-medium">Firmenname <span class="text-error">*</span></span>
                        </label>
                        <input
                          type="text"
                          id="company-name"
                          name="companyName"
                          value={user.companyName || ''}
                          class="input input-bordered input-sm"
                          required
                          placeholder="Ihre Firma GmbH"
                        />
                      </div>
                      <div class="form-control">
                        <label class="label py-1">
                          <span class="label-text text-sm font-medium">Umsatzsteuer-ID <span class="text-error">*</span></span>
                        </label>
                        <input
                          type="text"
                          id="vat-number"
                          name="vatNumber"
                          value={user.umsatzsteuernummer || ''}
                          class="input input-bordered input-sm"
                          required
                          placeholder="DE123456789"
                          pattern="^DE[0-9]{9}$"
                        />
                        <p class="text-xs text-base-content/40 mt-1.5">
                          Format: DE + 9 Ziffern
                        </p>
                      </div>
                    </div>

                    <div id="business-info-success" class="alert alert-success alert-sm hidden py-2">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                      </svg>
                      <span class="text-xs">Geschäftsinformationen erfolgreich aktualisiert!</span>
                    </div>

                    <div id="business-info-error" class="alert alert-error alert-sm hidden py-2">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
                      </svg>
                      <span class="text-xs" id="business-info-error-text">Fehler beim Aktualisieren der Informationen</span>
                    </div>

                    <button
                      type="submit"
                      class="btn btn-primary btn-sm gap-2"
                      id="save-business-info-button"
                    >
                      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
                      </svg>
                      Speichern
                    </button>
                  </form>
                </div>
              </div>

              <!-- Addresses -->
              <div class="card bg-base-200/50 shadow-md border border-base-300/30">
                <div class="card-body p-4">
                  <div class="flex items-center justify-between mb-3">
                    <h2 class="card-title text-lg flex items-center gap-2">
                      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-primary">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M15 10.5a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" />
                        <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1 1 15 0Z" />
                      </svg>
                      Adressen
                    </h2>
                    <button onclick="document.getElementById('add-address-modal').showModal()" class="btn btn-primary btn-sm gap-1.5">
                      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
                      </svg>
                      Neue Adresse
                    </button>
                  </div>

                  {userAddresses.length === 0 ? (
                    <div class="text-center py-8">
                      <div class="w-12 h-12 mx-auto mb-3 rounded-full bg-base-300/50 flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6 text-base-content/40">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M15 10.5a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" />
                          <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1 1 15 0Z" />
                        </svg>
                      </div>
                      <p class="text-sm text-base-content/60">Keine Adressen vorhanden. Fügen Sie eine Adresse hinzu, um Bestellungen aufzugeben.</p>
                    </div>
                  ) : (
                    <div class="space-y-3">
                      {userAddresses.map((address) => (
                        <div class="border border-base-300 rounded-lg p-3 hover:border-primary/50 transition-colors">
                          <div class="flex items-start justify-between gap-3">
                            <div class="flex-1">
                              <div class="flex items-center gap-2 mb-2">
                                <h3 class="font-semibold text-base-content">
                                  {address.firstName} {address.lastName}
                                </h3>
                                {address.isDefault && (
                                  <span class="badge badge-primary badge-xs">Standard</span>
                                )}
                                <span class="badge badge-ghost badge-xs">
                                  {address.addressType === 'shipping' && 'Lieferung'}
                                  {address.addressType === 'billing' && 'Rechnung'}
                                  {address.addressType === 'both' && 'Lieferung & Rechnung'}
                                </span>
                              </div>
                              {address.company && (
                                <p class="text-sm text-base-content/80">{address.company}</p>
                              )}
                              <p class="text-sm text-base-content/80">{address.addressLine1}</p>
                              {address.addressLine2 && (
                                <p class="text-sm text-base-content/80">{address.addressLine2}</p>
                              )}
                              <p class="text-sm text-base-content/80">
                                {address.postalCode} {address.city}
                                {address.stateProvince && `, ${address.stateProvince}`}
                              </p>
                              <p class="text-sm text-base-content/80">{address.country}</p>
                              {address.phone && (
                                <p class="text-sm text-base-content/60 mt-1">Tel: {address.phone}</p>
                              )}
                            </div>
                            <div class="flex gap-2">
                              <button
                                onclick={`editAddress(${JSON.stringify(address).replace(/"/g, '&quot;')})`}
                                class="btn btn-ghost btn-xs"
                                title="Bearbeiten"
                              >
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
                                  <path stroke-linecap="round" stroke-linejoin="round" d="m16.862 4.487 1.687-1.688a1.875 1.875 0 1 1 2.652 2.652L10.582 16.07a4.5 4.5 0 0 1-1.897 1.13L6 18l.8-2.685a4.5 4.5 0 0 1 1.13-1.897l8.932-8.931Zm0 0L19.5 7.125M18 14v4.75A2.25 2.25 0 0 1 15.75 21H5.25A2.25 2.25 0 0 1 3 18.75V8.25A2.25 2.25 0 0 1 5.25 6H10" />
                                </svg>
                              </button>
                              <button
                                onclick={`deleteAddress('${address.id}', '${address.firstName} ${address.lastName}')`}
                                class="btn btn-ghost btn-xs text-error"
                                title="Löschen"
                              >
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
                                  <path stroke-linecap="round" stroke-linejoin="round" d="m14.74 9-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 0 1-2.244 2.077H8.084a2.25 2.25 0 0 1-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 0 0-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 0 1 3.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 0 0-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 0 0-7.5 0" />
                                </svg>
                              </button>
                            </div>
                          </div>
                        </div>
                      ))}
                    </div>
                  )}
                </div>
              </div>

              <!-- Shipping Preferences -->
              <div class="card bg-base-200/50 shadow-md border border-base-300/30">
                <div class="card-body p-4">
                  <h2 class="card-title text-lg mb-3 flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-primary">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 18.75a1.5 1.5 0 0 1-3 0m3 0a1.5 1.5 0 0 0-3 0m3 0h6m-9 0H3.375a1.125 1.125 0 0 1-1.125-1.125V14.25m17.25 4.5a1.5 1.5 0 0 1-3 0m3 0a1.5 1.5 0 0 0-3 0m3 0h1.125c.621 0 1.129-.504 1.09-1.124a17.902 17.902 0 0 0-3.213-9.193 2.056 2.056 0 0 0-1.58-.86H14.25M16.5 18.75h-2.25m0-11.177v-.958c0-.568-.422-1.048-.987-1.106a48.554 48.554 0 0 0-10.026 0 1.106 1.106 0 0 0-.987 1.106v7.635m12-6.677v6.677m0 4.5v-4.5m0 0h-12" />
                    </svg>
                    Bevorzugte Versandart
                  </h2>
                  <p class="text-sm text-base-content/60 mb-4">Wählen Sie Ihre bevorzugte Versandart für zukünftige Bestellungen. Diese Einstellung wird automatisch beim Checkout vorausgewählt.</p>

                  {shippingProfiles.length === 0 ? (
                    <div class="text-center py-8">
                      <div class="w-12 h-12 mx-auto mb-3 rounded-full bg-base-300/50 flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6 text-base-content/40">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 18.75a1.5 1.5 0 0 1-3 0m3 0a1.5 1.5 0 0 0-3 0m3 0h6m-9 0H3.375a1.125 1.125 0 0 1-1.125-1.125V14.25m17.25 4.5a1.5 1.5 0 0 1-3 0m3 0a1.5 1.5 0 0 0-3 0m3 0h1.125c.621 0 1.129-.504 1.09-1.124a17.902 17.902 0 0 0-3.213-9.193 2.056 2.056 0 0 0-1.58-.86H14.25M16.5 18.75h-2.25m0-11.177v-.958c0-.568-.422-1.048-.987-1.106a48.554 48.554 0 0 0-10.026 0 1.106 1.106 0 0 0-.987 1.106v7.635m12-6.677v6.677m0 4.5v-4.5m0 0h-12" />
                        </svg>
                      </div>
                      <p class="text-sm text-base-content/60">Keine Versandarten verfügbar.</p>
                    </div>
                  ) : (
                    <div class="space-y-2" id="shipping-profiles-list">
                      {shippingProfiles.map((profile) => (
                        <label class="flex items-center justify-between p-3 border border-base-300 rounded-lg cursor-pointer hover:bg-base-200/50 transition-colors">
                          <div class="flex items-center gap-3 flex-1">
                            <input
                              type="radio"
                              name="shipping-profile"
                              value={profile.id}
                              checked={profile.id === selectedShippingId}
                              class="radio radio-primary radio-sm"
                              onchange={`updateShippingPreference('${profile.id}')`}
                            />
                            <div class="flex-1">
                              <div class="flex items-baseline gap-2">
                                <p class="font-medium text-base-content">{profile.name}</p>
                                {profile.isDefault && (
                                  <span class="badge badge-primary badge-xs">Standard</span>
                                )}
                              </div>
                              {profile.description && (
                                <p class="text-xs text-base-content/60 mt-0.5">{profile.description}</p>
                              )}
                              <div class="flex items-center gap-3 mt-1 text-xs text-base-content/60">
                                {profile.estimatedDays && (
                                  <span>Lieferung in ca. {profile.estimatedDays} {profile.estimatedDays === 1 ? 'Tag' : 'Tagen'}</span>
                                )}
                                {profile.freeShippingThreshold && (
                                  <span>• Kostenlos ab {new Intl.NumberFormat('de-DE', { style: 'currency', currency: 'EUR' }).format(profile.freeShippingThreshold)}</span>
                                )}
                              </div>
                            </div>
                          </div>
                          <div class="text-right ml-3">
                            <p class="font-semibold text-base-content">
                              {new Intl.NumberFormat('de-DE', { style: 'currency', currency: 'EUR' }).format(profile.basePrice)}
                            </p>
                          </div>
                        </label>
                      ))}
                    </div>
                  )}

                  <div id="shipping-success-message" class="hidden mt-3 alert alert-success py-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-5 w-5" fill="none" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <span class="text-sm">Versandpräferenz erfolgreich aktualisiert!</span>
                  </div>

                  <div id="shipping-error-message" class="hidden mt-3 alert alert-error py-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-5 w-5" fill="none" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <span class="text-sm" id="shipping-error-text">Fehler beim Aktualisieren der Versandpräferenz</span>
                  </div>
                </div>
              </div>

              <!-- Security Alert -->
              <div class="alert alert-info shadow-md py-3">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current shrink-0 w-5 h-5">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12.75 11.25 15 15 9.75m-3-7.036A11.959 11.959 0 0 1 3.598 6 11.99 11.99 0 0 0 3 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285Z" />
                </svg>
                <div class="flex-1">
                  <h3 class="font-semibold text-sm">Sicherheitshinweis</h3>
                  <div class="text-xs">Ihr Konto ist durch sichere Passwort-Verschlüsselung und Session-Management geschützt. Alle Login-Versuche werden auf verdächtige Aktivitäten überwacht.</div>
                </div>
              </div>
            </div>
        </div>
      </div>
    </div>
  </div>
  </div>

  <!-- Add Address Modal -->
  <dialog id="add-address-modal" class="modal modal-bottom sm:modal-middle">
    <div class="modal-box max-w-3xl shadow-2xl border border-base-300/50">
      <!-- Modal Header -->
      <div class="flex items-center justify-between pb-5 mb-7 border-b border-base-300/20">
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-primary">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
            </svg>
          </div>
          <div>
            <h3 class="font-bold text-xl text-base-content">Neue Adresse hinzufügen</h3>
            <p class="text-sm text-base-content/60 mt-0.5">Fügen Sie eine neue Liefer- oder Rechnungsadresse hinzu</p>
          </div>
        </div>
        <form method="dialog">
          <button class="btn btn-sm btn-circle btn-ghost hover:bg-base-300 transition-colors">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </form>
      </div>

      <form id="add-address-form" class="space-y-7">
        <!-- Personal Information Section -->
        <div>
          <div class="mb-5">
            <h4 class="text-sm font-semibold text-base-content uppercase tracking-wide mb-1">Persönliche Daten</h4>
            <p class="text-xs text-base-content/60">Geben Sie Ihre persönlichen Informationen ein</p>
          </div>
          
          <div class="grid grid-cols-1 md:grid-cols-2 gap-x-4 gap-y-4">
            <div class="form-control">
              <div class="mb-2">
                <label class="text-sm font-medium text-base-content">Vorname <span class="text-error">*</span></label>
              </div>
              <input type="text" name="firstName" class="input input-bordered focus:input-primary transition-all" placeholder="Max" required />
            </div>
            <div class="form-control">
              <div class="mb-2">
                <label class="text-sm font-medium text-base-content">Nachname <span class="text-error">*</span></label>
              </div>
              <input type="text" name="lastName" class="input input-bordered focus:input-primary transition-all" placeholder="Mustermann" required />
            </div>
          </div>
          
          <div class="form-control mt-4">
            <div class="mb-2">
              <label class="text-sm font-medium text-base-content">Firma <span class="text-xs text-base-content/50">(Optional)</span></label>
            </div>
            <input type="text" name="company" class="input input-bordered focus:input-primary transition-all" placeholder="Firma GmbH" />
          </div>
        </div>

        <!-- Address Information Section -->
        <div>
          <div class="mb-5">
            <h4 class="text-sm font-semibold text-base-content uppercase tracking-wide mb-1">Adressinformationen</h4>
            <p class="text-xs text-base-content/60">Geben Sie Ihre vollständige Lieferadresse ein</p>
          </div>
          
          <div class="space-y-4">
            <div class="form-control">
              <div class="mb-2">
                <label class="text-sm font-medium text-base-content">Straße und Hausnummer <span class="text-error">*</span></label>
              </div>
              <input type="text" name="addressLine1" class="input input-bordered focus:input-primary transition-all" placeholder="Musterstraße 123" required />
            </div>
            
            <div class="form-control">
              <div class="mb-2">
                <label class="text-sm font-medium text-base-content">Adresszusatz <span class="text-xs text-base-content/50">(Optional)</span></label>
                <p class="text-xs text-base-content/50 mt-0.5">z.B. Stockwerk, Appartment</p>
              </div>
              <input type="text" name="addressLine2" class="input input-bordered focus:input-primary transition-all" placeholder="2. Stock, Wohnung 5" />
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-x-4 gap-y-4">
              <div class="form-control">
                <div class="mb-2">
                  <label class="text-sm font-medium text-base-content">Postleitzahl <span class="text-error">*</span></label>
                </div>
                <input type="text" name="postalCode" class="input input-bordered focus:input-primary transition-all" placeholder="12345" required />
              </div>
              <div class="form-control">
                <div class="mb-2">
                  <label class="text-sm font-medium text-base-content">Stadt <span class="text-error">*</span></label>
                </div>
                <input type="text" name="city" class="input input-bordered focus:input-primary transition-all" placeholder="Berlin" required />
              </div>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-x-4 gap-y-4">
              <div class="form-control">
                <div class="mb-2">
                  <label class="text-sm font-medium text-base-content">Bundesland <span class="text-xs text-base-content/50">(Optional)</span></label>
                </div>
                <input type="text" name="stateProvince" class="input input-bordered focus:input-primary transition-all" placeholder="Berlin" />
              </div>
              <div class="form-control">
                <div class="mb-2">
                  <label class="text-sm font-medium text-base-content">Land <span class="text-error">*</span></label>
                </div>
                <select name="country" class="select select-bordered focus:select-primary transition-all" required>
                  <option value="DE">🇩🇪 Deutschland</option>
                  <option value="AT">🇦🇹 Österreich</option>
                  <option value="CH">🇨🇭 Schweiz</option>
                </select>
              </div>
            </div>
          </div>
        </div>

        <!-- Contact Information Section -->
        <div>
          <div class="mb-5">
            <h4 class="text-sm font-semibold text-base-content uppercase tracking-wide mb-1">Kontaktinformationen</h4>
            <p class="text-xs text-base-content/60">Für Rückfragen zur Lieferung</p>
          </div>
          
          <div class="form-control">
            <div class="mb-2">
              <label class="text-sm font-medium text-base-content">Telefon <span class="text-xs text-base-content/50">(Optional)</span></label>
              <p class="text-xs text-base-content/50 mt-0.5">Für Lieferbenachrichtigungen</p>
            </div>
            <input type="tel" name="phone" class="input input-bordered focus:input-primary transition-all" placeholder="+49 123 456789" />
          </div>
        </div>

        <!-- Options Section -->
        <div>
          <div class="mb-5">
            <h4 class="text-sm font-semibold text-base-content uppercase tracking-wide mb-1">Einstellungen</h4>
            <p class="text-xs text-base-content/60">Legen Sie die Standardoptionen fest</p>
          </div>
          
          <div class="bg-base-200/30 rounded-lg p-4 border border-base-300/30">
            <label class="label cursor-pointer justify-start gap-3 py-1">
              <input type="checkbox" name="isDefault" class="checkbox checkbox-primary" />
              <div>
                <span class="text-sm font-medium text-base-content">Als Standardadresse festlegen</span>
                <p class="text-xs text-base-content/50 mt-1">Diese Adresse wird automatisch beim Checkout ausgewählt</p>
              </div>
            </label>
          </div>
        </div>

        <!-- Modal Footer -->
        <div class="flex gap-3 pt-6 border-t border-base-300/30">
          <button type="button" class="btn btn-ghost flex-1" onclick="document.getElementById('add-address-modal').close()">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
            Abbrechen
          </button>
          <button type="submit" class="btn btn-primary flex-1 gap-2">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
            Hinzufügen
          </button>
        </div>
      </form>
    </div>
    <form method="dialog" class="modal-backdrop">
      <button>close</button>
    </form>
  </dialog>

  <!-- Edit Address Modal -->
  <dialog id="edit-address-modal" class="modal modal-bottom sm:modal-middle">
    <div class="modal-box max-w-3xl shadow-2xl border border-base-300/50">
      <!-- Modal Header -->
      <div class="flex items-center justify-between mb-7 pb-5 border-b border-base-300/20">
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-primary">
              <path stroke-linecap="round" stroke-linejoin="round" d="m16.862 4.487 1.687-1.688a1.875 1.875 0 1 1 2.652 2.652L10.582 16.07a4.5 4.5 0 0 1-1.897 1.13L6 18l.8-2.685a4.5 4.5 0 0 1 1.13-1.897l8.932-8.931Zm0 0L19.5 7.125M18 14v4.75A2.25 2.25 0 0 1 15.75 21H5.25A2.25 2.25 0 0 1 3 18.75V8.25A2.25 2.25 0 0 1 5.25 6H10" />
            </svg>
          </div>
          <div>
            <h3 class="font-bold text-xl text-base-content">Adresse bearbeiten</h3>
            <p class="text-sm text-base-content/60 mt-0.5">Aktualisieren Sie Ihre Adressinformationen</p>
          </div>
        </div>
        <form method="dialog">
          <button class="btn btn-sm btn-circle btn-ghost hover:bg-base-300 transition-colors">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </form>
      </div>

      <form id="edit-address-form" class="space-y-7">
        <input type="hidden" name="id" />
        
        <!-- Personal Information Section -->
        <div>
          <div class="mb-5">
            <h4 class="text-sm font-semibold text-base-content uppercase tracking-wide mb-1">Persönliche Daten</h4>
            <p class="text-xs text-base-content/60">Geben Sie Ihre persönlichen Informationen ein</p>
          </div>
          
          <div class="grid grid-cols-1 md:grid-cols-2 gap-x-4 gap-y-4">
            <div class="form-control">
              <div class="mb-2">
                <label class="text-sm font-medium text-base-content">Vorname <span class="text-error">*</span></label>
              </div>
              <input type="text" name="firstName" class="input input-bordered focus:input-primary transition-all" placeholder="Max" required />
            </div>
            <div class="form-control">
              <div class="mb-2">
                <label class="text-sm font-medium text-base-content">Nachname <span class="text-error">*</span></label>
              </div>
              <input type="text" name="lastName" class="input input-bordered focus:input-primary transition-all" placeholder="Mustermann" required />
            </div>
          </div>
          
          <div class="form-control mt-4">
            <div class="mb-2">
              <label class="text-sm font-medium text-base-content">Firma <span class="text-xs text-base-content/50">(Optional)</span></label>
            </div>
            <input type="text" name="company" class="input input-bordered focus:input-primary transition-all" placeholder="Firma GmbH" />
          </div>
        </div>

        <!-- Address Information Section -->
        <div>
          <div class="mb-5">
            <h4 class="text-sm font-semibold text-base-content uppercase tracking-wide mb-1">Adressinformationen</h4>
            <p class="text-xs text-base-content/60">Geben Sie Ihre vollständige Lieferadresse ein</p>
          </div>
          
          <div class="space-y-4">
            <div class="form-control">
              <div class="mb-2">
                <label class="text-sm font-medium text-base-content">Straße und Hausnummer <span class="text-error">*</span></label>
              </div>
              <input type="text" name="addressLine1" class="input input-bordered focus:input-primary transition-all" placeholder="Musterstraße 123" required />
            </div>
            
            <div class="form-control">
              <div class="mb-2">
                <label class="text-sm font-medium text-base-content">Adresszusatz <span class="text-xs text-base-content/50">(Optional)</span></label>
                <p class="text-xs text-base-content/50 mt-0.5">z.B. Stockwerk, Appartment</p>
              </div>
              <input type="text" name="addressLine2" class="input input-bordered focus:input-primary transition-all" placeholder="2. Stock, Wohnung 5" />
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-x-4 gap-y-4">
              <div class="form-control">
                <div class="mb-2">
                  <label class="text-sm font-medium text-base-content">Postleitzahl <span class="text-error">*</span></label>
                </div>
                <input type="text" name="postalCode" class="input input-bordered focus:input-primary transition-all" placeholder="12345" required />
              </div>
              <div class="form-control">
                <div class="mb-2">
                  <label class="text-sm font-medium text-base-content">Stadt <span class="text-error">*</span></label>
                </div>
                <input type="text" name="city" class="input input-bordered focus:input-primary transition-all" placeholder="Berlin" required />
              </div>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-x-4 gap-y-4">
              <div class="form-control">
                <div class="mb-2">
                  <label class="text-sm font-medium text-base-content">Bundesland <span class="text-xs text-base-content/50">(Optional)</span></label>
                </div>
                <input type="text" name="stateProvince" class="input input-bordered focus:input-primary transition-all" placeholder="Berlin" />
              </div>
              <div class="form-control">
                <div class="mb-2">
                  <label class="text-sm font-medium text-base-content">Land <span class="text-error">*</span></label>
                </div>
                <select name="country" class="select select-bordered focus:select-primary transition-all" required>
                  <option value="DE">🇩🇪 Deutschland</option>
                  <option value="AT">🇦🇹 Österreich</option>
                  <option value="CH">🇨🇭 Schweiz</option>
                </select>
              </div>
            </div>
          </div>
        </div>

        <!-- Contact Information Section -->
        <div>
          <div class="mb-5">
            <h4 class="text-sm font-semibold text-base-content uppercase tracking-wide mb-1">Kontaktinformationen</h4>
            <p class="text-xs text-base-content/60">Für Rückfragen zur Lieferung</p>
          </div>
          
          <div class="form-control">
            <div class="mb-2">
              <label class="text-sm font-medium text-base-content">Telefon <span class="text-xs text-base-content/50">(Optional)</span></label>
              <p class="text-xs text-base-content/50 mt-0.5">Für Lieferbenachrichtigungen</p>
            </div>
            <input type="tel" name="phone" class="input input-bordered focus:input-primary transition-all" placeholder="+49 123 456789" />
          </div>
        </div>

        <!-- Options Section -->
        <div>
          <div class="mb-5">
            <h4 class="text-sm font-semibold text-base-content uppercase tracking-wide mb-1">Einstellungen</h4>
            <p class="text-xs text-base-content/60">Legen Sie die Standardoptionen fest</p>
          </div>
          
          <div class="bg-base-200/30 rounded-lg p-4 border border-base-300/30">
            <label class="label cursor-pointer justify-start gap-3 py-1">
              <input type="checkbox" name="isDefault" class="checkbox checkbox-primary" />
              <div>
                <span class="text-sm font-medium text-base-content">Als Standardadresse festlegen</span>
                <p class="text-xs text-base-content/50 mt-1">Diese Adresse wird automatisch beim Checkout ausgewählt</p>
              </div>
            </label>
          </div>
        </div>

        <!-- Modal Footer -->
        <div class="flex gap-3 pt-6 border-t border-base-300/30">
          <button type="button" class="btn btn-ghost flex-1" onclick="document.getElementById('edit-address-modal').close()">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
            Abbrechen
          </button>
          <button type="submit" class="btn btn-primary flex-1 gap-2">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
            Speichern
          </button>
        </div>
      </form>
    </div>
    <form method="dialog" class="modal-backdrop">
      <button>close</button>
    </form>
  </dialog>
</Layout>

<style>
  /* Modern Modal Animations */
  .modal[open] .modal-box {
    animation: modal-slide-in 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  @keyframes modal-slide-in {
    from {
      transform: translateY(-20px) scale(0.95);
      opacity: 0;
    }
    to {
      transform: translateY(0) scale(1);
      opacity: 1;
    }
  }

  /* Enhanced Modal Backdrop */
  .modal-backdrop {
    backdrop-filter: blur(4px);
    background-color: rgba(0, 0, 0, 0.5);
  }

  /* Enhanced Input Borders */
  .modal-box .input,
  .modal-box .select,
  .modal-box .textarea {
    border-width: 1.5px;
    border-color: hsl(var(--bc) / 0.15);
  }

  .modal-box .input:hover,
  .modal-box .select:hover,
  .modal-box .textarea:hover {
    border-color: hsl(var(--bc) / 0.25);
  }

  /* Input Focus Enhancements */
  .modal-box .input:focus,
  .modal-box .select:focus,
  .modal-box .textarea:focus {
    transform: translateY(-1px);
    border-width: 1.5px;
    border-color: hsl(var(--p) / 0.6);
    box-shadow: 0 0 0 3px hsl(var(--p) / 0.1), 0 2px 8px rgba(0, 0, 0, 0.08);
    outline: none;
  }

  /* Smooth Transitions */
  .input,
  .select,
  .textarea,
  .btn {
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Enhanced Checkbox */
  .checkbox:checked {
    animation: checkbox-pop 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  @keyframes checkbox-pop {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.2);
    }
    100% {
      transform: scale(1);
    }
  }

  /* Checkbox with light green accent in modals */
  .modal-box .checkbox:checked:not(.checkbox-success):not(.checkbox-warning):not(.checkbox-error):not(.checkbox-info) {
    background-color: var(--color-accent) !important;
    border-color: var(--color-accent) !important;
  }

  .modal-box .checkbox:checked:not(.checkbox-success):not(.checkbox-warning):not(.checkbox-error):not(.checkbox-info):hover {
    background-color: var(--color-accent-dark) !important;
    border-color: var(--color-accent-dark) !important;
  }

  .modal-box .checkbox-primary:checked {
    background-color: var(--color-accent) !important;
    border-color: var(--color-accent) !important;
  }

  .modal-box .checkbox-primary:checked:hover {
    background-color: var(--color-accent-dark) !important;
    border-color: var(--color-accent-dark) !important;
  }

  /* Radio button with light green accent in modals */
  .modal-box .radio:checked:not(.radio-success):not(.radio-warning):not(.radio-error):not(.radio-info) {
    border-color: var(--color-accent) !important;
    background-color: var(--color-accent) !important;
  }

  .modal-box .radio:checked:not(.radio-success):not(.radio-warning):not(.radio-error):not(.radio-info)::before {
    background-color: white !important;
  }

  .modal-box .radio:checked:not(.radio-success):not(.radio-warning):not(.radio-error):not(.radio-info):hover {
    border-color: var(--color-accent-dark) !important;
    background-color: var(--color-accent-dark) !important;
  }

  .modal-box .radio-primary:checked {
    border-color: var(--color-accent) !important;
    background-color: var(--color-accent) !important;
  }

  .modal-box .radio-primary:checked::before {
    background-color: white !important;
  }

  .modal-box .radio-primary:checked:hover {
    border-color: var(--color-accent-dark) !important;
    background-color: var(--color-accent-dark) !important;
  }

  .modal-box .radio:focus,
  .modal-box .radio-primary:focus {
    outline-color: var(--color-accent) !important;
  }

  /* Section Dividers */
  .h-px {
    background: linear-gradient(90deg, transparent, currentColor, transparent);
  }

  /* Form Section Styling */
  .modal-box form > div:not(:last-child) {
    position: relative;
  }

  /* Section Headers */
  .modal-box form h4 {
    letter-spacing: 0.05em;
  }

  /* Grid Gap Consistency */
  .modal-box .grid {
    gap: 1rem;
  }

  .modal-box .grid.gap-x-4 {
    column-gap: 1rem;
  }

  .modal-box .grid.gap-y-4 {
    row-gap: 1rem;
  }
</style>

<script>
  import { authClient } from '../../lib/auth-client';

  const logoutButton = document.getElementById('logout-button');

  logoutButton?.addEventListener('click', async () => {
    try {
      await authClient.signOut();
      window.location.href = '/';
    } catch (error) {
      console.error('Logout error:', error);
      alert('Abmeldung fehlgeschlagen. Bitte versuchen Sie es erneut.');
    }
  });

  // Address management functions
  const addAddressForm = document.getElementById('add-address-form');
  const editAddressForm = document.getElementById('edit-address-form');

  // Add new address
  addAddressForm?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const formData = new FormData(addAddressForm);

    const data = {
      firstName: formData.get('firstName'),
      lastName: formData.get('lastName'),
      company: formData.get('company'),
      addressLine1: formData.get('addressLine1'),
      addressLine2: formData.get('addressLine2'),
      postalCode: formData.get('postalCode'),
      city: formData.get('city'),
      stateProvince: formData.get('stateProvince'),
      country: formData.get('country'),
      phone: formData.get('phone'),
      isDefault: formData.get('isDefault') === 'on',
      addressType: 'both',
    };

    try {
      const response = await fetch('/api/addresses/create', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });

      const result = await response.json();

      if (result.success) {
        document.getElementById('add-address-modal').close();
        window.location.reload();
      } else {
        alert('Fehler beim Hinzufügen der Adresse: ' + result.error.message);
      }
    } catch (error) {
      console.error('Error adding address:', error);
      alert('Fehler beim Hinzufügen der Adresse');
    }
  });

  // Edit address
  window.editAddress = (address) => {
    const form = document.getElementById('edit-address-form');
    form.querySelector('[name="id"]').value = address.id;
    form.querySelector('[name="firstName"]').value = address.firstName;
    form.querySelector('[name="lastName"]').value = address.lastName;
    form.querySelector('[name="company"]').value = address.company || '';
    form.querySelector('[name="addressLine1"]').value = address.addressLine1;
    form.querySelector('[name="addressLine2"]').value = address.addressLine2 || '';
    form.querySelector('[name="postalCode"]').value = address.postalCode;
    form.querySelector('[name="city"]').value = address.city;
    form.querySelector('[name="stateProvince"]').value = address.stateProvince || '';
    form.querySelector('[name="country"]').value = address.country;
    form.querySelector('[name="phone"]').value = address.phone || '';
    form.querySelector('[name="isDefault"]').checked = address.isDefault;

    document.getElementById('edit-address-modal').showModal();
  };

  // Update address
  editAddressForm?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const formData = new FormData(editAddressForm);

    const data = {
      id: formData.get('id'),
      firstName: formData.get('firstName'),
      lastName: formData.get('lastName'),
      company: formData.get('company'),
      addressLine1: formData.get('addressLine1'),
      addressLine2: formData.get('addressLine2'),
      postalCode: formData.get('postalCode'),
      city: formData.get('city'),
      stateProvince: formData.get('stateProvince'),
      country: formData.get('country'),
      phone: formData.get('phone'),
      isDefault: formData.get('isDefault') === 'on',
      addressType: 'both',
    };

    try {
      const response = await fetch('/api/addresses/update', {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });

      const result = await response.json();

      if (result.success) {
        document.getElementById('edit-address-modal').close();
        window.location.reload();
      } else {
        alert('Fehler beim Aktualisieren der Adresse: ' + result.error.message);
      }
    } catch (error) {
      console.error('Error updating address:', error);
      alert('Fehler beim Aktualisieren der Adresse');
    }
  });

  // Delete address
  window.deleteAddress = async (id, name) => {
    if (!confirm(`Möchten Sie die Adresse von ${name} wirklich löschen?`)) {
      return;
    }

    try {
      const response = await fetch('/api/addresses/delete', {
        method: 'DELETE',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ id }),
      });

      const result = await response.json();

      if (result.success) {
        window.location.reload();
      } else {
        alert('Fehler beim Löschen der Adresse: ' + result.error.message);
      }
    } catch (error) {
      console.error('Error deleting address:', error);
      alert('Fehler beim Löschen der Adresse');
    }
  };

  // Update shipping preference
  window.updateShippingPreference = async (shippingProfileId: string) => {
    const successMessage = document.getElementById('shipping-success-message');
    const errorMessage = document.getElementById('shipping-error-message');
    const errorText = document.getElementById('shipping-error-text');

    // Hide any existing messages
    successMessage?.classList.add('hidden');
    errorMessage?.classList.add('hidden');

    try {
      const response = await fetch('/api/cart/shipping/select', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ shippingProfileId }),
      });

      const result = await response.json();

      if (result.success) {
        // Show success message
        successMessage?.classList.remove('hidden');

        // Hide success message after 3 seconds
        setTimeout(() => {
          successMessage?.classList.add('hidden');
        }, 3000);
      } else {
        // Show error message
        if (errorText) {
          errorText.textContent = result.error?.message || 'Fehler beim Aktualisieren der Versandpräferenz';
        }
        errorMessage?.classList.remove('hidden');
      }
    } catch (error) {
      console.error('Error updating shipping preference:', error);
      if (errorText) {
        errorText.textContent = 'Fehler beim Aktualisieren der Versandpräferenz';
      }
      errorMessage?.classList.remove('hidden');
    }
  };

  // Business info form submission
  const businessInfoForm = document.getElementById('business-info-form');
  businessInfoForm?.addEventListener('submit', async (e) => {
    e.preventDefault();

    const successMessage = document.getElementById('business-info-success');
    const errorMessage = document.getElementById('business-info-error');
    const errorText = document.getElementById('business-info-error-text');
    const submitButton = document.getElementById('save-business-info-button');

    // Hide messages
    successMessage?.classList.add('hidden');
    errorMessage?.classList.add('hidden');

    // Disable submit button
    if (submitButton) {
      submitButton.disabled = true;
      submitButton.textContent = 'Wird gespeichert...';
    }

    const formData = new FormData(businessInfoForm as HTMLFormElement);
    const companyName = formData.get('companyName') as string;
    const vatNumber = formData.get('vatNumber') as string;

    try {
      // Validate VAT number format
      const vatRegex = /^DE[0-9]{9}$/;
      if (!vatRegex.test(vatNumber)) {
        throw new Error('Ungültige Umsatzsteuer-ID. Format: DE + 9 Ziffern (z.B. DE123456789)');
      }

      const response = await fetch('/api/auth/update-business-info', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          companyName,
          umsatzsteuernummer: vatNumber
        }),
      });

      const result = await response.json();

      if (result.success) {
        successMessage?.classList.remove('hidden');
        setTimeout(() => {
          successMessage?.classList.add('hidden');
        }, 3000);
      } else {
        throw new Error(result.error || 'Fehler beim Aktualisieren');
      }
    } catch (error) {
      console.error('Error updating business info:', error);
      const errorMsg = error instanceof Error ? error.message : 'Fehler beim Aktualisieren der Informationen';
      if (errorText) {
        errorText.textContent = errorMsg;
      }
      errorMessage?.classList.remove('hidden');
    } finally {
      if (submitButton) {
        submitButton.disabled = false;
        submitButton.innerHTML = `
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
          </svg>
          Speichern
        `;
      }
    }
  });
</script>
